import styled from "styled-components";

export default styled.div`
	min-height: 100vh;
	width: 100%;
	flex-wrap: wrap;
	padding: 100px 20px;
	.skills {
		width: 100%;
		display: flex;
		flex-wrap: wrap;
		color: var(--color4);
	}
`;
